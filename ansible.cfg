[defaults]
inventory = inventory.ini
remote_user = suitecrm
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root
